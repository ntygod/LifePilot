<?xml version="1.0" encoding="UTF-8"?>
<!--
  LifePilot 日志配置。
  CLI 模式下日志输出到文件，避免与 JLine 终端争抢 stdout 导致输入卡顿。
-->
<configuration>

    <property name="LOG_DIR" value="${user.home}/.lifepilot/logs" />
    <property name="LOG_FILE" value="${LOG_DIR}/lifepilot.log" />

    <!-- 文件 Appender：主日志输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/lifepilot.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 控制台 Appender：仅用于测试 profile -->
    <springProfile name="test">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <charset>UTF-8</charset>
                <pattern>%d{HH:mm:ss.SSS} %5level [%thread] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>

    <!-- 默认 profile（CLI 模式）：只输出到文件 -->
    <springProfile name="!test">
        <logger name="com.lifepilot" level="DEBUG" />
        <root level="INFO">
            <appender-ref ref="FILE" />
        </root>
    </springProfile>

</configuration>
