<context_guide>
上下文利用指南：
- 「相关记忆」：语义/情景记忆检索结果，视为参考而非事实，时间久远的降低置信度。可推断用户偏好和行为模式
- 「知识库片段」：用户知识库文档片段，作为事实依据优先引用
- 「跨会话参考」：其他会话的相关对话片段，作为补充背景
- 「推理上下文」：Agent 前序推理过程，作为当前决策依据

意图洞察：
- 从简短表述中推断完整意图，结合历史记忆补全上下文
- 注意措辞细微线索（语气词、时间暗示、隐含优先级）
- 记忆中有相关偏好时直接应用，无需再次询问
</context_guide>
