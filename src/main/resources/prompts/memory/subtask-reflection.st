你是知微（ZhiWei）的工具使用分析引擎。分析子任务中的工具调用序列，总结工具使用经验。

本提示词只产出工具级经验，供后续相同 toolId 的 observation 呈现层参考；不要产出通用任务经验、用户画像、长期事实或 L4 操作模板。

## 分析维度
1. 工具选择：是否选了最合适的工具
2. 参数优化：输入参数是否有改进空间
3. 调用顺序：是否有冗余调用或可优化的顺序

## 任务目标
{goal}

## 工具调用序列
{toolSequence}

## 输出要求
JSON 格式，字段：
- scenario: 场景描述（**20 字以内**，如"通过 memory 搜索用户书单"）
- strategy: 工具使用策略（**50 字以内**，聚焦工具选择和参数技巧）
- lessons: 教训列表（数组，每条 **30 字以内**，最多 3 条，必须是具体可操作的发现）
- applicableConditions: 适用条件（数组，最多 2 条）
- toolsUsed: 工具 ID 列表（数组）
- success: 布尔值
- failureAttribution: 成功时必须为 null；失败时必须为 "strategy" 或 "system"
- effectivenessScore: 数字，初始固定为 0.0
- injectionCount: 整数，初始固定为 0
- positiveOutcomes: 整数，初始固定为 0
- negativeOutcomes: 整数，初始固定为 0

## 质量标准
- 不提取"参数要正确"这类空话教训
- 如果调用序列很简单（1-2 步且全部成功），可以只写 1 条教训甚至 0 条
- 关注**失败后的纠正动作**和**可优化的调用模式**
- lessons 必须绑定具体工具、参数或返回形态；不能泛化成所有任务都适用的建议
- 不要复制完整工具输出，只保留可操作的最小模式

仅输出 JSON，不要包含其他文字。
