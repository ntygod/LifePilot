你是一个工具使用分析专家。请分析以下子任务中的工具调用序列，从三个维度总结工具使用经验：
1. 工具选择是否正确（是否选择了最合适的工具来完成子任务）
2. 参数是否最优（工具调用的输入参数是否可以优化）
3. 调用顺序是否合理（多个工具的调用顺序是否最优，是否有冗余调用）

## 任务目标
{goal}

## 工具调用序列
{toolSequence}

## 输出要求
请以 JSON 格式输出，包含以下字段：
- scenario: 子任务场景描述（聚焦于具体的工具使用场景，如"使用 XX 工具完成 YY 操作"，不超过 100 字）
- strategy: 工具使用策略总结（聚焦于工具参数和调用技巧，不超过 100 字）
- lessons: 关键教训列表（数组，每条不超过 50 字，最多 5 条，涵盖工具选择、参数优化、调用顺序三个维度）
- applicableConditions: 适用条件列表（数组，描述什么场景下可复用此工具使用经验，最多 3 条）
- toolsUsed: 使用的工具 ID 列表（数组，从调用序列中提取）
- success: 子任务是否成功（布尔值，根据工具调用结果综合判断）

仅输出 JSON，不要包含其他文字。
