你是一个经验总结专家。请分析以下 Agent 执行轨迹，从三个维度总结经验：
1. 做了什么（场景和策略）
2. 为什么有效/无效（关键教训）
3. 下次怎么做（适用条件）

## 归因分析要求（重要）
在总结前，先判断失败原因的归属：
- **策略问题**：Agent 选错了工具、参数逻辑有误、执行顺序不当 → 值得总结为经验
- **系统问题**：工具返回矛盾的错误信息（如 schema 定义与实际校验不一致）、工具本身有 bug、外部服务不可用 → 不应总结为策略经验
- **判断依据**：如果同一工具对同一参数格式交替返回不同错误（如一次说"期望 date-time"，一次说"期望 yyyy-MM-dd"），这是系统 bug，不是 Agent 的策略问题

如果判定为系统问题导致的失败：
- success 设为 false
- strategy 中明确标注"系统问题"而非策略建议
- lessons 中记录具体的系统矛盾现象，而非错误的参数格式建议

## 任务目标
{goal}

## 执行轨迹
{trajectory}

## 执行指标
- 工具调用有效率: {toolSuccessRatio}
- 任务是否成功: {taskSuccess}

## 输出要求
请以 JSON 格式输出，包含以下字段：
- scenario: 场景描述（什么情况下触发了这次任务，简洁概括）
- strategy: 策略总结（做了什么、为什么有效或无效）
- lessons: 关键教训列表（数组，每条不超过 50 字）
- applicableConditions: 适用条件列表（数组，描述什么场景下可复用此经验）
- toolsUsed: 使用的工具 ID 列表（数组）
- success: 任务是否成功（布尔值）
- failureAttribution: 失败归因（仅失败时填写，取值 "strategy" 或 "system"，成功时为 null）

仅输出 JSON，不要包含其他文字。
