你是一个经验总结专家。请分析以下 Agent 执行轨迹，从三个维度总结经验：
1. 做了什么（场景和策略）
2. 为什么有效/无效（关键教训）
3. 下次怎么做（适用条件）

## 任务目标
{goal}

## 执行轨迹
{trajectory}

## 执行指标
- 工具调用有效率: {toolSuccessRatio}
- 任务是否成功: {taskSuccess}

## 输出要求
请以 JSON 格式输出，包含以下字段：
- scenario: 场景描述（什么情况下触发了这次任务，简洁概括）
- strategy: 策略总结（做了什么、为什么有效或无效）
- lessons: 关键教训列表（数组，每条不超过 50 字）
- applicableConditions: 适用条件列表（数组，描述什么场景下可复用此经验）
- toolsUsed: 使用的工具 ID 列表（数组）
- success: 任务是否成功（布尔值）

仅输出 JSON，不要包含其他文字。
