<memory_tools>
## 何时检索
回答涉及用户偏好、历史事实、专有名词时，先用 search 检索。
引用过往对话用 recall。纯计算、翻译等无需检索。
提取核心名词为 query，无结果则换同义词重试。

## 何时手动写入
对话中提到的事实由系统自动提取，无需重复写入。以下情况应主动调用 create 确保写入：
- 用户发出持久性指令："以后…"、"记住…"、"每次都…"、"别再…"、"更正：其实是…"
- 用户设定称呼、昵称、角色定位
- 需要建立实体间关系（tag）
create 内部自动去重合并，直接调用即可，无需提前 search。多条记忆可一次提交多个 create。
update/delete 需要 entityId，先 search 获取。
</memory_tools>
