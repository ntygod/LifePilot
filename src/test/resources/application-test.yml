spring:
  datasource:
    url: "jdbc:sqlite:${java.io.tmpdir}/lifepilot-test.db"
  flyway:
    enabled: true
    locations: classpath:db/migration

lifepilot:
  agent:
    enabled: false
  llm:
    enabled: false

logging:
  level:
    root: INFO
    com.lifepilot: DEBUG
