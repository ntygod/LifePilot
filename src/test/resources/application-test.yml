spring:
  datasource:
    url: "jdbc:sqlite:${java.io.tmpdir}/lifepilot-test.db"
  flyway:
    enabled: true
    locations: classpath:db/migration

lifepilot:
  llm:
    enabled: false

logging:
  level:
    root: INFO
    com.lifepilot: DEBUG
